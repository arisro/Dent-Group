<div class="navbar navbar-fixed-top" data-activeslide="1">
	<div class="container">
		<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
		</button>

		<div class="nav-collapse collapse navbar-responsive-collapse">
			<ul class="nav row">
				<%= render 'layouts/navigation_links' %>
			</ul>
			<div class="row">
				<div class="col-sm-2 active-menu"></div>
			</div>
			<!--ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome <%= current_user.email %><b class="caret"></b></a>
					<ul class="dropdown-menu">
						<li><%= link_to 'Edit profile', edit_user_registration_path %></li>
						<li class="divider"></li>
						<li><%= link_to "Logout", destroy_user_session_path, method: :delete  %></li>
					</ul>
				</li>
					<% else %>
						<%= simple_form_for(:user, :url => user_session_path, :html => { id: "loginForm", class: 'form-inline' }) do |f| %>
						  	<div class="form-group">
						   		<%= f.input :email, :required => false, :autofocus => true, placeholder: "E-mail address", :input_html => { :class => "form-control" }, label_html: { class: "sr-only" }  %>
						   	</div>
						   	<div class="form-group">
						    	<%= f.input :password, :required => false, placeholder: "Password", :input_html => { :class => "form-control" }, label_html: { class: "sr-only" } %>
						    </div>
						    <div class="checkbox">
						    	<%= f.input :remember_me, :as => :boolean if devise_mapping.rememberable? %>
						    </div>

						    <%= f.button :submit, "Sign in", class: "btn btn-default" %>
						    <%= link_to "Sign up", new_user_registration_path, class: 'btn btn-default'  %>
						<% end %>
				<% end %>
			</ul-->
		</div>
	</div>
</nav>
